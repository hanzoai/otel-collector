name: build
on:
  push:
    tags:
      - v*

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      docker_providers: ${{ steps.set-docker-providers.outputs.providers }}
    steps:
      - name: set-docker-providers
        id: set-docker-providers
        run: |
          if [[ ${{ github.event.ref }} =~ ^refs/tags/v[0-9]+\.[0-9]+\.[0-9]+$ || ${{ github.event.ref }} =~ ^refs/tags/v[0-9]+\.[0-9]+\.[0-9]+-rc\.[0-9]+$ ]]; then
            echo "providers=dockerhub gcp" >> $GITHUB_OUTPUT
          else
            echo "providers=gcp" >> $GITHUB_OUTPUT
          fi
  hanzo-otel-collector:
    uses: o11y/primus.workflows/.github/workflows/go-build.yaml@main
    secrets: inherit
    needs: prepare
    with:
      PRIMUS_REF: main
      GO_VERSION: 1.24
      GO_BUILD_CONTEXT: ./cmd/o11yotelcollector
      GO_BUILD_FLAGS: ""
      DOCKER_BASE_IMAGES: '{"debian":"debian:bookworm-slim"}'
      DOCKER_DOCKERFILE_PATH: ./cmd/o11yotelcollector/Dockerfile.multi-arch
      DOCKER_MANIFEST: true
      DOCKER_PROVIDERS: ${{ needs.prepare.outputs.docker_providers }}
  o11y-schema-migrator:
    uses: o11y/primus.workflows/.github/workflows/go-build.yaml@main
    secrets: inherit
    needs: prepare
    with:
      PRIMUS_REF: main
      GO_VERSION: 1.24
      GO_NAME: o11y-schema-migrator
      GO_BUILD_CONTEXT: ./cmd/o11yschemamigrator
      GO_BUILD_FLAGS: ""
      DOCKER_BASE_IMAGES: '{"debian":"debian:bookworm-slim"}'
      DOCKER_DOCKERFILE_PATH: ./cmd/o11yschemamigrator/Dockerfile.multi-arch
      DOCKER_MANIFEST: true
      DOCKER_PROVIDERS: ${{ needs.prepare.outputs.docker_providers }}
